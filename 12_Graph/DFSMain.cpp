/*
#include "ALGraph.h"
#include <iostream>

int DFSMain(void)
{
    ALGraph graph;
    GraphInit(&graph, 4);

    AddEdge(&graph, A, B);
    AddEdge(&graph, A, C);
    AddEdge(&graph, A, D);

    ShowGraphEdgeInfo(&graph);

    DFShowGraphVertex(&graph, A); printf("\n");

    GraphDestroy(&graph);
    return 0;
}
*/